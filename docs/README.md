# docs/

Long-form project documentation. For *how to work in this repo* (conventions,
guardrails, workflows), the source of truth is **`CLAUDE.md`** at the root, plus
`CLAUDE-REFERENCE.md`. This folder holds the deeper references.

## Current references
| File | Read it for |
|---|---|
| [`product-doctrine.md`](product-doctrine.md) | **The wedge, anti-drift rules, surface hierarchy, and the feature decision test.** Read before any product/design/IA/engine work. |
| [`feelflick-foundation-readiness-audit.md`](feelflick-foundation-readiness-audit.md) | **F0 foundation audit** — ground-truth baseline + the F1–F10 rebuild roadmap. Start here for the rebuild. |
| [`ia-v2-decision-record.md`](ia-v2-decision-record.md) | **F2 IA v2 decision record** — how the nav encodes the surface hierarchy (the Briefing is primary; browse/lists/discover are supporting). The source of truth for nav structure. |
| [`design-system-hardening-f3.md`](design-system-hardening-f3.md) | **F3 design-system hardening** — brand vs semantic tokens, what drift was retired vs retained (and why), Eyebrow/stash decisions. |
| [`landing-onboarding-vnext-f4.md`](landing-onboarding-vnext-f4.md) | **F4 landing + onboarding vNext** — landing-primitive adoption (Eyebrow/AuthCTA/Wordmark), Community honesty framing, onboarding "why we ask" copy, visual re-baseline status. |
| [`home-briefing-vnext-f5.md`](home-briefing-vnext-f5.md) | **F5 home / Briefing vNext** — surfacing the "why this pick" case (`WhyThisPick`), Briefing loading skeleton, supporting-tail hierarchy decision; engine untouched. |
| [`discover/f3-summary.md`](discover/f3-summary.md) | **F3 `/discover` redesign — final summary** (closure ledger for F3.1–F3.14): why it was rebuilt, the final mood-first → optional-context → 900ms-resolve → one-pick journey, the verified per-phase ledger (PRs + SHAs), product transformation, final architecture, trust/a11y/fallback-truth outcomes, frozen contracts, the test/E2E/visual evidence + interception safety model, and the **private/beta-ready** verdict. See also [`discover/beta-readiness-f312.md`](discover/beta-readiness-f312.md). |
| [`home/f4-summary.md`](home/f4-summary.md) | **F4 `/home` redesign — final summary** (closure ledger for F4.1–F4.9): why it was rebuilt (a second, louder Discover wrapped in an 8-section dashboard), the final automatic pick-first → grounded-or-no reason → actions → demoted "Adjust mood" → QuickLog → quiet Discover-exit journey, the verified per-phase ledger (PRs + SHAs), product transformation, final architecture, trust/a11y/reliability outcomes, frozen contracts, the 101-unit / 962-full / 14-E2E / 8-visual evidence + interception/no-live-write model, the **private/beta-ready** verdict, and the deferred register. |
| [`film-file-case-making-f6a.md`](film-file-case-making-f6a.md) | **F6A Film File case-making design** — current-state map, tiered case hierarchy, UI/data-contract plan, F6B options. Corrects the F0 "one seeded film" framing. Read before F6B. |
| [`film-file-case-making-f6b.md`](film-file-case-making-f6b.md) | **F6B Film File case-making UI** — what shipped: `PrimaryCaseCard` (consolidated tier-aware case), `ViewerNotes` honest reframe, Parasite-placeholder cleanup. UI-only. |
| [`cinematic-dna-profile-vnext-f7.md`](cinematic-dna-profile-vnext-f7.md) | **F7 Cinematic DNA / Profile vNext** — removed cold-state fabrications (fake taste/skew/YIR), honest `DnaConfidence` framing (evidence, not accuracy). `dnaConfidence` formula untouched. |
| [`recommendation-trust-evaluation-f8a.md`](recommendation-trust-evaluation-f8a.md) | **F8A Recommendation Trust + Evaluation foundation** — current-state map, the 6-family metrics framework, the offline fixture harness (`shared/services/eval/`), read-only SQL templates (`sql/recommendation-evaluation-queries.sql`), the explanation-quality rubric, real-data baseline (outcome-capture gap), and the tuning gates. **Evaluation-only — engine untouched.** |
| [`recommendation-outcome-capture-f8b.md`](recommendation-outcome-capture-f8b.md) | **F8B Recommendation Outcome Capture Repair** — fixes the F8A capture gap: current-state map of every outcome write, the attribution contract (recency-gated `recordRecommendationOutcome`), the save/watch/click wiring repair, schema reused (no migration), tests, read-only verification SQL (§7), and the F8C tuning gate. **Instrumentation-only — engine untouched.** |
| [`release-readiness-f9a.md`](release-readiness-f9a.md) | **F9A Release / CI / Production Hardening Prep** — branch lineage, the local+CI validation matrix, the Linux visual-baseline blocker + exact regeneration command, e2e/a11y status, deploy/preview + rollback + smoke + post-deploy outcome-capture checklists, and the restated F8C gate. **Docs/validation-only — no code change.** (Linux baseline resolved in F9B.) |
| [`post-merge-smoke-f9c.md`](post-merge-smoke-f9c.md) | **F9C Merge / Deploy / Smoke / Outcome baseline** — the rebuild merged to `main` (#169, squash `c38cb473`) + deployed to production; post-merge CI + local validation; the live production smoke results (public + authenticated); **proof that F8B outcome capture works in production** (recency-gated save attribution flipped real impression flags); the post-deploy baseline + why **F8C stays blocked**; and remaining hardening follow-ups (incl. the Sentry-ingest 403). |
| [`production-observability-security-f9d.md`](production-observability-security-f9d.md) | **F9D Production Observability + Security Headers** — diagnoses the Sentry 403 (Allowed-Domains inbound filter: 200 server-to-server, 403 with any browser Origin → one-time Sentry dashboard fix, no code/env change); ships safe security headers via `public/_headers` (Cloudflare Pages — the prod path) + mirrored `vercel.json`; defers CSP with a draft report-only policy; documents the CI secret names for E2E/Lighthouse. **Config + docs only — no product/engine change.** Notes prod is served by **Cloudflare Pages**, not Vercel. |
| [`post-f9d-verification-f9e.md`](post-f9d-verification-f9e.md) | **F9E Post-F9D verification** — merged F9D (PR #171, squash `0b9a1b5c`); **security headers verified LIVE on `app.feelflick.com`** (all five present, app renders, fonts/TMDB load, no header/CSP breakage); Sentry still 403 (read-only Sentry MCP can't write the Allowed-Domains filter → manual dashboard step documented). Verification only — no code change. |
| [`sentry-ingest-verification-f9f.md`](sentry-ingest-verification-f9f.md) | **F9F Sentry ingest verification** — after the user applied the Allowed-Domains fix: the **403 is gone** (server-side `app.feelflick.com`/`*.feelflick.com` → 200, control `example.com` → 403; browser envelope 200; clean prod console), and a **labeled runtime test error landed in Sentry Issues** (env `production`, url `app.feelflick.com`, project `feelflick-app`) confirmed via the read-only MCP. Replay masks text; no PII exposed. Verification only — no code change. |
| [`csp-report-only-f9g.md`](csp-report-only-f9g.md) | **F9G CSP report-only** — ships `Content-Security-Policy-Report-Only` via `public/_headers` (+ `vercel.json`): the full source inventory (Supabase/TMDB/Google Fonts/Sentry/Cloudflare/YouTube/OAuth), the exact policy (`style-src 'unsafe-inline'` required; `script-src` kept strict), reports to the Sentry security endpoint (verified 200), why enforcement is deferred, expected noise, the pre-enforcement cleanup list, and rollback. **Report-only — never blocks; no enforcing CSP.** |
| [`csp-report-only-verification-f9g1.md`](csp-report-only-verification-f9g1.md) | **F9G.1 CSP report-only production verification** — merged F9G (PR #174, squash `86a0ebab`); confirmed `content-security-policy-report-only` is **live on prod** (no enforcing CSP; other headers intact); app renders + fonts/TMDB/Supabase/Sentry all work; the **only** violation is a Cloudflare-injected inline script (`script-src-elem`, Sentry `FEELFLICK-APP-5`); reporting pipeline verified end-to-end. Verification only — no code change. |
| [`cloudflare-rum-csp-cleanup-f9g2.md`](cloudflare-rum-csp-cleanup-f9g2.md) | **F9G.2 Cloudflare inline-script CSP blocker — diagnosis** — corrects F9G.1: the inline violation is **Cloudflare JavaScript Detections** (Bot Management `__CF$cv$params`/`/cdn-cgi/challenge-platform/jsd/`), **not** the RUM beacon (which is external + already allowed). It's per-request + intermittent + self-removing → **hash-pinning non-viable**. No Cloudflare zone-settings access from the repo → documents the fix options (disable JSD if plan allows; Cloudflare-recommended **per-request nonce via a Pages Function**; `'unsafe-inline'` last resort) + exact manual path + rollback. Diagnosis/docs only — CSP stays report-only; enforcement deferred. |
| [`cloudflare-csp-nonce-f9g3.md`](cloudflare-csp-nonce-f9g3.md) | **F9G.3 Cloudflare JSD resolution — per-request nonce (Pages Function)** — implements Option B: a new `functions/_middleware.js` emits a per-request CSP nonce on HTML responses so Cloudflare auto-nonces its JSD inline script (no `'unsafe-inline'`; `script-src` stays strict); the static CSP moves out of `public/_headers` (F9D headers stay). Verified on the CF Pages preview; the JSD-nonce effect is prod-only. Still report-only. |
| [`csp-nonce-production-verification-f9g4.md`](csp-nonce-production-verification-f9g4.md) | **F9G.4 CSP nonce — production verification** — merged F9G.3 (PR #177, squash `8f68a235`); confirmed on prod that the report-only CSP carries a **rotating per-request nonce** (single header, no enforcing, F9D intact) and that **Cloudflare's JSD inline script is now nonced** → the `script-src-elem` violation is **gone** (console clean, no CSP report POST, Sentry `FEELFLICK-APP-5` stopped getting events). App/JSD/Sentry all work. **CSP now reports zero violations → enforcement eligible after a short monitoring window** (+ add `report-to`, drop `child-src`). Verification only. |
| [`ci-real-gates-verification-f9h1.md`](ci-real-gates-verification-f9h1.md) | **F9H.1 Enable real CI gates (E2E + Lighthouse)** — performs what F9H documented: uploads the 5 repo secrets from approved local sources (piped over stdin to `gh secret set` — no value printed/written/committed; presence verified via `gh secret list`) and adds `collect.url: ["http://localhost/index.html"]` to `lighthouserc.json` so lhci audits the SPA shell only (not the `google…html` verification stub). Both gates now run for real (CI-confirmed on the PR). Local: lint · 487 tests · build · audit green; E2E 14/14. One-line config change; workflows untouched. F8C still blocked. |
| [`ci-nonskip-gates-f9h.md`](ci-nonskip-gates-f9h.md) | **F9H Non-skip CI gates (E2E + Lighthouse)** — documents why the E2E + Lighthouse jobs are "skip-green" (missing repo secrets), the exact secrets to add (`E2E_TEST_EMAIL/PASSWORD`, `VITE_SUPABASE_URL/ANON_KEY`, `VITE_TMDB_API_KEY`) + `gh secret set` steps (no values printed/committed), the recommended gate strategy, and enablement caveats (the Lighthouse `accessibility: error ≥0.9` would PASS — landing scores 0.96 — but `staticDistDir` also audits the google-verification stub). Both workflows are structurally sound → **docs only, no workflow change**; they auto-flip to real gates once secrets exist. Local: E2E 14/14, build/test green. |
| [`private-preview-plan-f10a.md`](private-preview-plan-f10a.md) | **F10A Private Preview Plan + Decision Criteria** — scope of a small invite-only preview (5–10 trusted cold+warm film-watchers), what feedback is useful, what NOT to ask testers yet, preview success/blocker criteria, the **F8C volume gate** (≥5 real users · ~300–500+ hero/discover impressions on a stable `algorithm_version 2.17` · ≥3 days), and the post-preview decision criteria. **Operations/measurement — no runtime change; not a public launch.** |
| [`private-preview-tester-guide-f10a.md`](private-preview-tester-guide-f10a.md) | **F10A Tester guide** — the non-technical guide sent to preview testers: what FeelFlick is, what to try first, sign-in/onboarding, using Tonight (save/skip/open), the Film File, Profile/DNA, what feedback helps, known limitations, privacy note, don't-share-publicly. |
| [`private-preview-launch-runbook-f10a.md`](private-preview-launch-runbook-f10a.md) | **F10A Launch runbook (internal)** — pre-invite checklist; production smoke; **Sentry / CSP-security / CI / Supabase outcome-capture** checks (read-only, methods from F9E/F9F/F9G.4); tester invite sequence + per-tester onboarding smoke; daily monitoring; rollback/pause criteria; feedback triage + issue taxonomy (P0/P1/P2/insight/rec-quality); the mid-preview freeze. |
| [`private-preview-feedback-template-f10a.md`](private-preview-feedback-template-f10a.md) | **F10A Feedback template** — the tester feedback form (mood→pick relevance, "why this pick", save/open/skip/watch, Film File trust, DNA honesty, confusion, "felt fake/overconfident?", come-back-tomorrow) + a structured bug-report section. |
| [`outcome-baseline-collection-f10a.md`](outcome-baseline-collection-f10a.md) | **F10A Outcome baseline collection plan** — the measurement spine: which §7 SQL to run + when, how to segment (placement · `algorithm_version` · cold/warm · new/auth · mood), the metrics to watch (shown→clicked/saved/watched, clicked→conv, skip, repeated-pick, generic-reason, orphan, `outcomeCaptureRate`), what counts as "capture working" vs "**enough for F8C**", and the recorded **pre-preview dev baseline** (8 users / 3,376 impressions — capture on hero+discover, 0 on carousels; dev-only). |
| [`private-preview-wave-1-f10b.md`](private-preview-wave-1-f10b.md) | **F10B Private Preview, Wave 1 (operations)** — the live operating sheet for the first wave (2–3 testers): selection criteria, invite sequence, **tester invite copy** (DM/email/reminder/feedback), the **recorded pre-invite readiness gate** (prod 200, headers, CSP-RO nonce, Sentry clean, CI green, Cloudflare healthy, Supabase auth + read-only baseline), per-tester onboarding smoke, daily monitoring checklist, baseline/measurement plan + warning signs, data-freeze + triage + pause rules, Wave-2 success criteria. **Operations only — engine frozen `2.17`; not a public launch.** |
| [`private-preview-wave-1-tracker-template-f10b.md`](private-preview-wave-1-tracker-template-f10b.md) | **F10B Wave 1 tracker (blank template)** — per-tester fields (alias, cohort, device, invited/accepted/signed-in/onboarded/pick-seen/why-seen/Film-File/save-skip-watch/feedback, issue category, follow-up), the onboarding-smoke checklist, and a daily log. **Aliases only — no real tester PII in the repo.** |
| [`architecture.md`](architecture.md) | Current system architecture — verified against code in F1 (routes, folders, engine, auth, services, external touchpoints, risks). |
| [`FeelFlick_Overview.md`](FeelFlick_Overview.md) | Product overview in prose — features + the engine (reconciled in F1; framed as features in service of the single pick). |
| [`product-research-patterns.md`](product-research-patterns.md) | Competitive patterns to **borrow / refuse**, mapped to the wedge (research-framing memo). |
| [`DESIGN_SYSTEM.md`](DESIGN_SYSTEM.md) | World-class product-design patterns reference (aspirational; the *enforced* design rules live in `CLAUDE.md`). |
| [`PLANNING.md`](PLANNING.md) | The active-sprint slice of the phased rebuild. |
| [`user-journey.md`](user-journey.md) | End-to-end user journey — ⚠️ **stale** (references removed `/x-legacy` routes + a `HeroSection` that no longer exists); pending a later refresh. |
| [`SUPABASE_SCHEMA.md`](SUPABASE_SCHEMA.md) | DB schema reference — ⚠️ auto-generated snapshot (pre-2026-05-29); the live DB is the source of truth. |

## Structured
- [`decisions/`](decisions/) — Architecture Decision Records (ADRs) + `DECISIONS.md` index.
- [`audits/`](audits/) — point-in-time audit write-ups.
- [`runbooks/`](runbooks/) — operational runbooks (dev setup, debugging).
- [`sql/`](sql/) — **read-only** analysis query templates (e.g. `recommendation-evaluation-queries.sql`).
- [`eval/`](eval/) — generated evaluation baselines (fixture-based; regenerate via `scripts/eval/`).
- [`ui/`](ui/) — **F11A UI consistency + visual design audit** (audit/direction only — **no UI implementation; engine frozen**): `ui-inspiration-translation-f11a.md` (borrow disciplines not visuals), `ui-inventory-f11a.md` (code-grounded tokens/primitives), `ui-consistency-audit-f11a.md` (drift P0–P3), `route-visual-audit-f11a.md` (12 routes), `feelflick-visual-direction-f11a.md` (the visual direction + do-not-become), `claude-ui-skills-recommendation-f11a.md` (safe tooling, install nothing 3rd-party), `ui-polish-implementation-plan-f11b.md` (the 6-wave F11B roadmap), **`design-tokens-primitives-f11b1.md`** (F11B.1 — additive tokens + `Card` primitive), and the **F11B.2** rhythm docs `page-rhythm-walkthrough-f11b2.md` (code-grounded audit + scope) + `page-rhythm-home-movie-profile-f11b2.md` (the zero-pixel `SPACE`/`RADIUS` token adoption in the Home/Movie/Profile trust callouts), and the **F11B.3** docs `authenticated-visual-walkthrough-f11b3.md` (a REAL live authenticated visual pass via the Playwright harness — finding: routes already coherent) + `authenticated-card-rhythm-f11b3.md` (the zero-pixel `RADIUS` migration across the Movie/Profile section files; padding-normalization + `<Card>` deferred), and the **F11B.4** doc `accent-panel-trust-surfaces-f11b4.md` (the `<AccentPanel>` primitive — accent-tinted sibling of `Card` — + the byte-identical WhyThisPick proof migration), and the **F11B.5** doc `accent-panel-gradient-primarycase-f11b5.md` (the constrained `variant="gradient"` that encodes the PrimaryCaseCard directional glow + its byte-identical proof migration; DnaConfidence still deferred). **F12A** premium UI visual-QA audit (13 routes × 6 breakpoints, authenticated, AUDIT ONLY): `premium-visual-qa-protocol-f12a.md` (method + rubric), `premium-route-visual-audit-f12a.md` (route-by-route), `premium-component-audit-f12a.md` (buttons/cards/containers/type), `responsive-composition-matrix-f12a.md` (route×breakpoint), `premium-ui-backlog-f12a.md` (F12B–F12F prioritized), `ui-skills-tooling-f12a.md` (tooling eval), `f12b-premium-polish-plan.md` (proposed next phase = container + type-scale + h1s). **F12B** (first polish wave, implemented): `page-container-type-h1-f12b.md` (the `<PageContainer>` primitive + `LAYOUT`/`GUTTER`/`TYPE` tokens + the 4 sr-only route `<h1>`s + byte-identical Account/History container adoption). **F12C** (mobile touch pass, implemented): `mobile-touch-density-f12c.md` (the mobile-only `.ff-tap`/`.ff-tap-hit` 44px touch-floor utilities applied to Browse/History/Account/Home control bars; History per-item buttons + Home skeleton deferred). **F12D** (button system, implemented): `button-cta-system-f12d.md` (shared `<Button>` even min-height floor sm40/md44/lg48 + CTA hierarchy discipline; `/about` baseline unchanged → no rebaseline; font→Outfit still deferred). **F12E** (card/poster/History-row rhythm, implemented): `card-poster-history-row-f12e.md` (solved the F12C-deferred History diary remove control → real 44×44 in its grid column, 0 overlap/overflow verified; title button = accepted redundant-with-poster exception; card/poster audit → restraint, no MovieCard/poster change). **F12F** (motion/micro-interactions, implemented): `motion-microinteractions-f12f.md` (RESTRAINT — additive `MOTION` tokens + reduced-motion guard: global `prefers-reduced-motion: reduce` reset, Button scale `motion-safe:`-gated, `.ff-tap` tactile press; no `/about` rebaseline; MovieCard hover-LAW untouched). Start with the visual direction + the F11B plan + the F12A backlog.
- [`personas/`](personas/) — **F10C Synthetic Persona Usability Lab** (UX inspection, **synthetic ≠ real users**, does **not** unblock F8C): `persona-schema-f10c.md`, `synthetic-personas-f10c.md` (16 archetypes), `persona-usability-tasks-f10c.md`, `persona-usability-rubric-f10c.md`, `claude-persona-test-prompts-f10c.md`, `persona-testing-tooling-proposal-f10c.md` (docs-only), `persona-pilot-findings-f10c.md` (2-persona pilot), and **`persona-usability-backlog-f10c1.md`** (F10C.1 — pilot findings classified into validate-with-real-users / safe-UX-candidate / protect-do-not-regress / defer; nothing acted on). **F10D full 16-persona simulation:** `full-synthetic-simulation-protocol-f10d.md`, `full-synthetic-persona-findings-f10d.md` (all 16), `synthetic-cohort-findings-f10d.md` (cross-persona), `synthetic-outcome-matrix-f10d.md` (simulated predictions — **not data**), `synthetic-ux-backlog-f10d.md` (5 buckets), and **`real-preview-watch-items-from-simulation-f10d.md`** (the key output — what to validate in Wave 1). Start with the schema; run via the prompt pack; triage via the backlogs; **the watch-items doc is what carries into the real preview.**

## archive/
Historical / superseded docs — see [`archive/README.md`](archive/README.md). Not current.

export default {
  plugins: {
    // Tailwind v4's PostCSS plugin (replaces the v3 `tailwindcss` + `autoprefixer`
    // pair — vendor prefixing is now built in).
    '@tailwindcss/postcss': {},
  },
}
